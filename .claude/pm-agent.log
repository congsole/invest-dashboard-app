이슈가 생성되었습니다.

**`issues/002-email-verification-flow/issue.md`** 생성 완료

주요 내용:
- `EmailVerificationScreen` 구현 (안내 문구, 재발송 버튼 60초 쿨다운, 로그인 복귀 버튼)
- 회원가입 후 닉네임 임시 보관 → `SIGNED_IN` 이벤트 수신 시 프로필 저장
- `expo-linking` 및 `app.json` scheme 설정 (`investdashboard://`)
- 이메일 미인증 로그인 에러 처리

다음 단계로 파이프라인을 계속 진행할까요? (`domain-model-agent` → `db-schema-agent` → `api-spec-agent` → 병렬 구현)
