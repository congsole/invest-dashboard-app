# 백엔드 통합 테스트

## 테스트 환경
- Supabase URL: wxznscebhpkfspwphbdv.supabase.co
- 테스트 파일: app/tests/integration/019-memo-sector-filter.test.ts

## 테스트 결과

| # | 테스트 케이스 | 결과 |
|---|-------------|------|
| 019-1-1 | L1 섹터 필터 시 L4 섹터 종목 연결 메모 포함 (계층 탐색) | 통과 |
| 019-1-2 | L1 섹터 필터 시 L2 섹터 종목 연결 메모 포함 | 통과 |
| 019-1-3 | L2 섹터 필터 시 해당 L2 종목 메모 포함 | 통과 |
| 019-1-4 | L4 섹터 필터 시 해당 L4 종목 메모 포함 | 통과 |
| 019-1-5 | 다른 L1 섹터 필터 시 IT 종목 메모 제외 / 다른 섹터 메모 포함 | 통과 |
| 019-2-1 | L1 섹터 필터 시 L1 직접 연결 메모 포함 | 통과 |
| 019-2-2 | L1 섹터 필터 시 하위 L4 직접 연결 메모 포함 (하위 탐색) | 통과 |
| 019-2-3 | L4 섹터 필터 시 L4 직접 연결 메모 포함 | 통과 |
| 019-2-4 | 다른 L1 섹터 필터 시 IT 직접 연결 메모 제외 | 통과 |
| 019-3-1 | L1 섹터 필터 시 종목 경로 + 직접 연결 경로 메모 모두 포함 (UNION) | 통과 |
| 019-3-2 | 두 경로 모두 해당하는 메모가 중복 없이 1회만 포함 | 통과 |
| 019-3-3 | 섹터 연결 없는 메모는 어느 필터에도 포함되지 않음 | 통과 |
| 019-3-4 | 복수 섹터 [L1, otherL1] OR 동작으로 합집합 반환 | 통과 |
| 019-4-1 | L1 지정 시 L2 섹터 하위 종목 연결 메모 포함 | 통과 |
| 019-4-2 | L1 지정 시 L4 섹터 하위 종목 연결 메모 포함 | 통과 |
| 019-4-3 | L2 지정 시 L4 섹터 하위 종목 연결 메모 포함 (L2→L3→L4 탐색) | 통과 |
| 019-4-4 | L1 지정 시 L1/L4 직접 연결 memo_sectors 메모 포함 | 통과 |
| 019-5-1 | sectors 배열에 sector_id, code, name, name_en, level 포함 확인 | 통과 |
| 019-5-2 | L1 직접 연결 메모의 sectors[].level = 1 | 통과 |
| 019-5-3 | L4 직접 연결 메모의 sectors[].level = 4 | 통과 |
| 019-5-4 | sectors[].name_en이 null이 아닌 문자열 | 통과 |
| 019-5-5 | 섹터 연결 없는 메모의 sectors = 빈 배열 | 통과 |
| 019-6-7-1 | create L1 섹터 연결 — 응답 sectors[].level=1, name_en 포함 | 통과 |
| 019-6-7-2 | create L2 섹터 연결 — 응답 sectors[].level=2 | 통과 |
| 019-6-7-3 | create L3 섹터 연결 — 응답 sectors[].level=3 | 통과 |
| 019-6-7-4 | create L4 섹터 연결 — 응답 sectors[].level=4 | 통과 |
| 019-6-7-5 | create 복수 레벨(L1+L4) 동시 연결 — 응답에 모두 포함 | 통과 |
| 019-6-7-6 | 존재하지 않는 sector_id로 create 시 에러 반환 | 통과 |
| 019-6-7-7 | sectors 없이 create 시 응답 sectors = 빈 배열 | 통과 |
| 019-6-7-8 | 미인증 사용자 create_memo_with_links 호출 불가 | 통과 |
| 019-8-9-1 | update L1 섹터 — 응답 sectors[].level=1, name_en 포함 | 통과 |
| 019-8-9-2 | update L4 섹터 — 응답 sectors[].level=4, name_en 문자열 | 통과 |
| 019-8-9-3 | update 빈 배열 — 응답 sectors = 빈 배열 | 통과 |
| 019-8-9-4 | update p_sector_ids=null — 섹터 연결 변경 없음 | 통과 |
| 019-8-9-5 | 존재하지 않는 sector_id로 update 시 에러 반환 | 통과 |
| 019-8-9-6 | 다른 유저의 메모 update 시 에러 반환 (소유권 확인) | 통과 |
| 019-10-1 | p_sector_ids 필터 시 total_count = 실제 반환 메모 수 | 통과 |
| 019-10-2 | p_sector_ids=null 시 total_count = 실제 메모 총 수 | 통과 |
| 019-10-3 | 페이지네이션 시 offset 변경에도 total_count 동일 | 통과 |
| 019-10-4 | 미인증 사용자 list_memos 호출 불가 | 통과 |

**전체: 40개 통과 / 0개 실패**

## 수정 내역
없음 — 초기 실행에서 전체 통과.

## 최종 결과
통과
