# 프론트엔드 구현 내역

## 새로 만든 화면
없음

## 새로 만든 컴포넌트
없음

## 수정한 파일
- `app/components/MemoFilter.tsx` — 필터 영역 레이아웃을 세 줄 구조로 재구성

## UI 레퍼런스 매핑
UI 레퍼런스 파일 없음 (PRD-003-memo.md 기획서 직접 참조)

## 변경 내용 요약

### 레이아웃 구조 변경
기존: 단일 가로 스크롤 ScrollView에 모든 필터 칩 나열
변경 후: 세 줄 구조

| 행 | 내용 |
|---|---|
| 1행 (토글 행) | 매매 이벤트 토글 / 뉴스 연관 토글 / 연결 없음 토글 / 초기화 버튼 |
| 2행 (종목 행) | "종목" 헤더 레이블 + 가로 스크롤 (선택된 종목 칩 + 종목 선택 버튼) |
| 3행 (섹터 행) | "섹터" 헤더 레이블 + 가로 스크롤 섹터 칩 |

### 레이블 변경
- "매매 연관" → "매매 이벤트" (PRD 용어와 일치)

### "연결 없음" 위치 이동
- 기존: 가로 스크롤 마지막 위치
- 변경: 1행 토글 행에 배치 (매매 이벤트 / 뉴스 연관과 동일 행)

### 필터 동작 로직
변경 없음. 기존 결합 규칙 유지:
- 같은 행 내 복수 선택 → OR (종목, 섹터)
- 다른 행/타입 간 → AND
- "연결 없음" 선택 시 다른 필터 자동 해제 (상호 배타적)

## 특이사항
- API 시그니처 변경 없음 (list_memos RPC 파라미터 그대로)
- `MemoListScreen.tsx` 수정 불필요 — MemoFilter props 인터페이스 변경 없음
- 기존 `FilterChip`, `SectorFilterChip`, `SelectedStockChip` 내부 컴포넌트 재사용
- 1행 토글 행은 ScrollView 없이 `flexDirection: 'row'` + `gap`으로 배치 (토글 3개 고정 개수)
- 2행, 3행은 가로 스크롤이 필요하므로 ScrollView 유지
- `rowLabel` 스타일에 고정 width(28) 지정으로 종목/섹터 행 칩 시작 위치 정렬
